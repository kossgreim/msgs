
<div class="row">
  	<% @users.each do |user| %>
  	<div>
		<%= image_tag user.avatar.url(:thumb) %><br>
		<%= link_to user.name, '' %><br>

		<%= add_if_user_not_current(user, current_user) %>
	</div>
	<hr><br>
	<% end %>		
</div>