<h4>New Conversation</h4>


<%= form_for(@conversation) do |f| %>
  <div>
    <%= f.label :to %><br />
    <% for user in @friends do %>
      <%= check_box_tag "user_conversation[to][]", user.friend.id %> <%= user.friend.name %><br />
    <% end %>
    <br>
  </div>
  <%= f.fields_for :conversation do |c| %>
    <div>
      <%= c.label :subject %>
      <%= c.text_field :subject %>
    </div>
    <%= c.fields_for :messages do |m| %>
      <div>
        <%= m.label :body %><br />
        <%= m.text_area :body %>
      </div>
    <% end %>
  <% end %>

  <%= f.submit %>
<% end %>